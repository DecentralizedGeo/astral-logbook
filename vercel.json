{
  "buildCommand": "cd packages/nextjs && yarn build && node -e \"require('fs').writeFileSync('.next/export-detail.json', '{}')\"",
  "outputDirectory": "packages/nextjs/.next",
  "installCommand": "yarn install --frozen-lockfile"
}
