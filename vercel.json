{
  "buildCommand": "cd packages/nextjs && yarn build",
  "outputDirectory": "packages/nextjs/.next",
  "installCommand": "yarn install --frozen-lockfile",
  "framework": "nextjs",
  "functions": {
    "api/storage/storacha/files/*.js": {
      "maxDuration": 60
    }
  }
}
